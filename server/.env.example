# Conexão com o PostgreSQL.
# Copie este arquivo para ".env" e ajuste com os dados do banco de vocês.

PGHOST=localhost
PGPORT=5432
PGDATABASE=restaurante
PGUSER=postgres
PGPASSWORD=postgres

# Porta em que a API vai rodar
PORT=3001
